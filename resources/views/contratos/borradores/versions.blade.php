<x-app-layout>
    <x-slot name="header"><div class="flex justify-between"><h2 class="font-semibold text-xl text-gray-800">Historial del borrador #{{ $draft->id }}</h2><a href="{{ route('contratos.borradores.show', $draft) }}" class="bg-gray-500 hover:bg-gray-700 text-white font-bold py-2 px-4 rounded-lg">Volver al borrador</a></div></x-slot>
    <div class="max-w-6xl mx-auto mt-6 bg-white rounded-lg shadow p-5 overflow-x-auto"><table class="min-w-full text-sm"><thead class="border-b"><tr><th class="text-left p-3">Versión</th><th class="text-left p-3">Acción</th><th class="text-left p-3">Actor</th><th class="text-left p-3">Fecha</th><th class="text-left p-3">Hash</th><th></th></tr></thead><tbody>@foreach($versions as $version)<tr class="border-b"><td class="p-3">{{ $version->draft_version }}</td><td class="p-3">{{ $version->action }}</td><td class="p-3">{{ $version->createdBy?->name ?? '—' }}</td><td class="p-3">{{ $version->created_at->format('Y-m-d H:i') }}</td><td class="p-3 font-mono text-xs">{{ $version->payload_hash }}</td><td class="p-3"><a class="text-blue-600 underline" href="{{ route('contratos.borradores.version', [$draft, $version]) }}">Ver snapshot</a></td></tr>@endforeach</tbody></table></div>
</x-app-layout>
